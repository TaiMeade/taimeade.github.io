# Dependencies
node_modules/

# Build output (regenerated by CI / `npm run build`)
dist/

# Local env overrides — `.env` itself is committed on purpose (public EmailJS
# IDs, needed at build time); these per-machine overrides should not be.
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Misc
todo.txt
