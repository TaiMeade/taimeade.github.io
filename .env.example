# Copy to .env and fill in for each site. All three values come from your
# EmailJS dashboard and are safe to commit (they are public client-side IDs).
VITE_EMAILJS_PUBLIC_KEY=your_public_key
VITE_EMAILJS_SERVICE_ID=your_service_id
VITE_EMAILJS_TEMPLATE_ID=your_template_id
